[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anshu Kumar", email = "anshu.kumar726@gmail.com" }
]
requires-python = ">=3.11.8"
dependencies = [
    "marker-pdf>=2.0.0",
    "ollama>=0.6.2",
]

[project.scripts]
scripts = "scripts:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
